<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User's Blogs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
   Assuming you have a CSS file named styles.css in the public directory
</head>
<body class="bg-dark text-light">
    <h1 class="text-center fw-light fw-italic mt-5" style="font-size:5rem ;">User's Blogs</h1>
    
        <ul class="m-5" style="list-style-type: none; ">
            <% blogs.forEach(blog => { %>
                <li class="container text-center mt-5 border bg-secondary text-white">
                    <h2 style="font-family: elsie; font-size: 10rem;"><%= blog.title %></h2>
                    <h3><%= blog.subtitle %></h3>
                    <p class="bg-light text-dark mt-5 p-5"><%= blog.body %></p>
                    <p><strong>Author:</strong> <%= blog.author %></p>
                </li>
            <% }); %>
        </ul>
   
    
</body>
</html> -->


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User's Blogs</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Assuming you have a CSS file named styles.css in the public directory -->
</head>
<body>
    <h1>User's Blogs</h1>
    <ul>
        <% blogs.forEach(blog => { %>
            <li>
                <h2><%= blog.title %></h2>
                <h3><%= blog.subtitle %></h3>
                <p><%= blog.body %></p>
                <p><strong>Author:</strong> <%= blog.author %></p>
                <!-- Preview button to view the blog post on another page -->
                <a href="/blog/<%= encodeURIComponent(blog.title) %>">Preview</a>
                <!-- Edit link -->
                <a href="/edit/<%= blog._id %>">Edit</a>
                <!-- Delete form -->
              
                    
                
            </li>
        <% }); %>
    </ul>
</body>
</html>